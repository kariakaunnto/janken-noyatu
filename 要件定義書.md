# ジャンケン迎撃タイムアタック 要件定義書

## 0. このドキュメントについて

「要件定義書」とは、**「これから作るものが、どんなもので、何ができればいいか」を文章にまとめたもの**です。
プログラムを書き始める前にこれを整理しておくと、

- 作っている途中で「あれ、何を作ろうとしていたんだっけ?」と迷わなくなる
- 後から機能を追加するときに、全体像を見失わずに済む

というメリットがあります。今回はすでに動くもの(`index.html`)を先に作ってしまいましたが、
**「実際に作ったものを正式な文書として書き起こす」** という体験として読んでみてください。

---

## 1. 概要(このゲームは何か)

ブラウザ上で遊べる、じゃんけんをテーマにした反射神経系のミニゲーム。
画面上部からグー・チョキ・パーの手が降ってきて、プレイヤーは「それに勝つ手」を入力して迎撃する。
サーバーやインストールは不要で、`index.html` をブラウザで開くだけで遊べる。

## 2. 目的

- プログラミング初心者が、HTML・CSS・JavaScript の基本(画面切り替え、イベント処理、タイマー、アニメーション)を
  1つの題材を通して学べる教材にする
- 「タイムアタック」という要素を入れることで、遊んでいて達成感・繰り返しプレイしたくなる感覚を作る

## 3. 対象ユーザー

- パソコンのブラウザ(Chrome / Edge / Safari など)でプレイする人
- キーボード操作、またはマウス/タッチ操作のどちらでも遊べることを想定

---

## 4. 機能要件(このゲームが「できること」の一覧)

「機能要件」= プログラムが実際に動作として満たすべき条件のこと。

### 4-1. スタート画面

- ゲームタイトルと簡単な遊び方の説明を表示する
- 以下の3つのモードから1つを選んでゲームを開始できる

| モード名 | 内容 | 終了条件 |
|---|---|---|
| ①サバイバル | 制限時間60秒の間、何体倒せるか挑戦する | 60秒経過、または残機(3つ)が0になる |
| ②固定数クリア | 30体を倒すのにかかった時間を競う | 30体倒す、または残機(3つ)が0になる |
| ③難易度上昇 | 倒すたびに落下速度・出現間隔が速くなっていく、無限モード | 1回でもミスしたら終了(残機1) |

### 4-2. ゲーム画面

- 画面は3つの「レーン(縦の通り道)」に分かれており、各レーンにランダムに手(✊/✌️/✋)が出現し、下に落下してくる
- 手が画面下の「判定ライン」に到達する前に、正しい手を入力して迎撃する必要がある
- 入力方法は次の2通りに対応する(どちらを使ってもよい)
  - キーボード: `Z`キー=グー、`X`キー=チョキ、`C`キー=パー
  - 画面上のボタンをクリック/タップ
- 迎撃に成功すると、対象の手は消える演出(拡大して消える)とともにスコアが+1される
- 落下中の手が複数ある場合、**判定ラインに一番近い(=一番危険な)手を優先して倒す**
- 判定ラインに到達してしまう(=倒しきれなかった)と「ミス」となり、残機が1つ減る
- 画面上部には常に「スコア」「残機」「残り時間 or 経過時間」を表示する

### 4-3. 結果画面

- ゲーム終了時、モードに応じた結果メッセージを表示する
  - サバイバル: 「倒した数」
  - 固定数クリア: 「クリアタイム」、または未達成なら「倒した数 / 目標数」
  - 難易度上昇: 「倒した数」
- 「同じモードでもう一度遊ぶ」「モード選択画面に戻る」の2つのボタンを用意する

---

## 5. 画面遷移(画面の移り変わり)

```
[スタート画面]
      │ モードを選ぶ
      ▼
[ゲーム画面] ──(条件達成 or 残機0)──▶ [結果画面]
      ▲                                   │
      └───────── 「もう一度」ボタン ────────┘
                       │
      「モード選択に戻る」ボタン
                       ▼
                [スタート画面]
```

---

## 6. 非機能要件(遊び心地やシステム面の条件)

「非機能要件」= 「何ができるか」ではなく「どういう品質・環境で動くべきか」という条件のこと。

- インターネット接続不要、サーバー不要(HTMLファイルを開くだけで動く)
- 追加のライブラリやツールのインストールが不要(HTML/CSS/JavaScriptのみで完結)
- スマートフォンなどタッチ操作の端末でも、ボタンタップで遊べること
- 1ファイル(`index.html`)で完結しており、コードの流れを追いやすいこと(教材としての読みやすさを優先)

---

## 7. 用語集(このゲーム・このドキュメントに出てくる言葉)

| 用語 | 意味 |
|---|---|
| 要件定義書 | これから作るものの仕様・条件をまとめた文書 |
| 機能要件 | プログラムが備えるべき「できること」の条件 |
| 非機能要件 | 動作環境や使いやすさなど、「できること」以外の品質条件 |
| レーン | 手が落下してくる縦方向の通り道(このゲームでは3本) |
| 判定ライン | これを手が通過するとミスになる、画面下部の赤い線 |
| 残機 | あと何回ミスできるかを表す数(ハートの数で表示) |
| タイムアタック | 制限時間やタイムを競うゲーム形式のこと |

---

## 8. 今後の拡張アイデア(実装は未定)

- ハイスコア・ベストタイムをブラウザに保存する機能
- 迎撃成功時の効果音、BGMの追加
- コンボ(連続成功)によるスコア倍率
- 難易度上昇モードで「現在の速度レベル」を表示する

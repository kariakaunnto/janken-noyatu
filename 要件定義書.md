# ジャンケン迎撃タイムアタック 要件定義書

## 0. このドキュメントについて

「要件定義書」とは、**「作ったものが、どんなもので、何ができるか」を文章にまとめたもの**です。
この文書は、これまでの開発・デバッグ・機能追加を経て**何度も内容を更新してきました**。
最初に作った時から比べると、ルールも見た目もかなり変わっているので、
「今、実際に動いているものは何か」を正しく把握するために読んでみてください。

---

## 1. 概要(このゲームは何か)

ブラウザ上で遊べる、じゃんけんをテーマにした反射神経系のミニゲーム。
画面上部からグー・チョキ・パーの手(イラスト画像)が降ってきて、プレイヤーは
「一番下まで落ちてきている1体」に対してジャンケンを仕掛け、迎撃していく。
サーバーやインストールは不要で、`index.html` をブラウザで開くだけで遊べる。
GitHub Pages上でも公開しており、URLを開くだけでスマホからも遊べる。

## 2. 目的

- プログラミング初心者が、HTML・CSS・JavaScript の基本(画面切り替え、イベント処理、タイマー、
  アニメーション、Web Audio API、Gitでの公開)を1つの題材を通して学べる教材にする
- 「タイムアタック」という要素を入れることで、遊んでいて達成感・繰り返しプレイしたくなる感覚を作る

## 3. 対象ユーザー

- パソコン・スマートフォンどちらのブラウザでもプレイできる人
- キーボード操作、またはマウス/タッチ操作のどちらでも遊べることを想定

---

## 4. 機能要件(このゲームが「できること」の一覧)

「機能要件」= プログラムが実際に動作として満たすべき条件のこと。

### 4-1. スタート画面

- ゲームタイトルと簡単な遊び方の説明を表示する
- **敵のスピード調整スライダー**: 0.2倍〜2.0倍まで0.2刻みで選べる(デフォルト1.0倍)。
  選んだ倍率は、落下速度(後述の`fallDuration`)にそのまま反映される
- 以下の3つのモードから1つを選んでゲームを開始できる

| モード名 | 内容 | 終了条件 |
|---|---|---|
| ①サバイバル | 制限時間30秒の間、何体倒せるか挑戦する | 30秒経過、または残機が0になる |
| ②固定数クリア | 30体を倒すのにかかった時間を競う | 30体倒す、または残機が0になる |
| ③難易度上昇 | 倒すたびに落下速度・出現間隔が速くなっていく、無限モード | 残機が0になるまで継続 |

- **初期残機はどのモードも共通で10**(以前は3または1だったが、後述の「負け」でも
  残機が減るルールに変更したため、10に引き上げた)

### 4-2. ゲーム画面

#### 出現・落下のしくみ

- 画面は3つの「レーン(縦の通り道)」に分かれている
- 各レーンには同時に1体までしか敵が出現しない(空いているレーンを確認してから
  **0.25秒待って**出現させることで、演出中の敵と新しい敵が重ならないようにしている)
- 出現した敵(グー/チョキ/パーのイラスト画像)は、指定の時間(`fallDuration`)をかけて
  レーンの上から下まで落下する
- 判定ライン(赤い線)はレーンの本当の下端と同じ位置にあり、そこを敵の本体が越える前に
  対処できなければ「ミス」になる

#### 入力とジャンケン判定(重要: 通常のジャンケンゲームとは判定方式が異なる)

- 入力方法は次の2通りに対応する(どちらを使ってもよい)
  - キーボード: `Z`キー=グー、`X`キー=チョキ、`C`キー=パー
  - 画面上のボタンをクリック/タップ(押すと一瞬色が変わり、押されたことが分かる)
- **判定対象は「今画面に出ている敵の中で、一番落下が進んでいる(一番危険な)1体」だけ**。
  手の種類に関係なく、進み具合(経過時間 ÷ 落下時間)が最も高いものが対象になる
- 入力した手と、対象の敵の手を比べて、以下の3パターンに分岐する

| 結果 | 条件 | 効果 |
|---|---|---|
| 勝ち | 入力した手が対象に勝つ | 対象の敵を撃破(スコア+1、コンボ+1)。「回復っぽい」上昇音が鳴る |
| あいこ | 入力した手が対象と同じ | 何も起きない。「キャンセルっぽい」短い音が鳴るのみ |
| 負け | 入力した手が対象に負ける | 残機-1。「バシッ」という打撃音が鳴り、画面の端が赤く一瞬光る。**対象の敵は消えずそのまま落下を続ける** |

- 対象の敵に何もしないまま判定ラインを越えると「ミス」になり、これも残機-1(負けた時と同じ
  打撃音・画面フラッシュが発生する)
- 撃破に成功すると、対象の敵はその場で位置を止め、グレースケール(白黒)になりながら拡大する
  演出を0.25秒再生してから消える(演出中に次の敵が同じレーンに出ないよう、消えてからレーンを
  空き状態に戻す)

#### コンボ

- 「勝ち」が連続するたびにコンボ数が+1される
- 「負け」「ミス」、または対象がいない状態で入力してハズレた場合はコンボが0にリセットされる
  (「あいこ」はリセットされない = ノーリスクで様子見できる)
- コンボが3以上になると、HUDのコンボ表示が金色になり強調される
- プレイ中の最大コンボ数(`maxCombo`)を記録しておき、結果画面に表示する

#### HUD(画面上部の表示)

常に以下を表示する。コンボ・残機は表示文字数が変わっても位置がずれないよう、幅を固定して左寄せにしている。

- スコア(倒した数。固定数クリアモードのみ「スコア: n / 30」の形式)
- コンボ(現在の連続成功数)
- 残機(ハートの数)
- 残り時間(サバイバル)、または経過時間(固定数クリア・難易度上昇)

#### 効果音(Web Audio APIでその場で生成、音声ファイルは使わない)

- 勝ち: 回復っぽい柔らかいサイン波の上昇音
- あいこ: キャンセルっぽいフラットな短い二連音
- 負け・ミス: 「バシッ」という打撃音(ノイズ+ローパスフィルター)
- 固定数クリア達成: 明るい上昇アルペジオのファンファーレ

### 4-3. 結果画面

- ゲーム終了時、モードに応じた結果メッセージと「最大コンボ」「ミス回数」を表示する
  - サバイバル: 「倒した数」
  - 固定数クリア: 「クリアタイム」、または未達成なら「倒した数 / 30体」
  - 難易度上昇: 「倒した数」
- 「同じモードでもう一度遊ぶ」「モード選択画面に戻る」の2つのボタンを用意する

---

## 5. 画面遷移(画面の移り変わり)

```
[スタート画面]
      │ スピードとモードを選ぶ
      ▼
[ゲーム画面] ──(条件達成 or 残機0)──▶ [結果画面]
      ▲                                   │
      └───────── 「もう一度」ボタン ────────┘
                       │
      「モード選択に戻る」ボタン
                       ▼
                [スタート画面]
```

---

## 6. 非機能要件(遊び心地やシステム面の条件)

「非機能要件」= 「何ができるか」ではなく「どういう品質・環境で動くべきか」という条件のこと。

- インターネット接続不要、サーバー不要(HTMLファイルを開くだけで動く)。
  GitHub Pagesにも公開済みで、URLを開くだけでも遊べる
- 追加のライブラリやツールのインストールが不要(HTML/CSS/JavaScript + 画像3枚のみで完結)
- スマートフォンでの利用を前提にレスポンシブ対応済み
  - `viewport`メタタグを設定し、実際の画面幅で正しく描画されるようにしている
  - 全ボタンに`touch-action: manipulation`を指定し、ダブルタップ拡大ジェスチャーによる
    タップの取りこぼしを防止
  - タイトル画面のボタンは`:hover`を`@media (hover: hover)`で囲み、iOS Safari特有の
    「1回目のタップがホバー扱いになる」不具合を回避
- 落下する手の見た目は、環境依存のカラー絵文字ではなく、独自に用意した透過PNG画像
  (`gu.png` / `choki.png` / `pa.png`)を使用し、環境によって見え方が変わらないようにしている

---

## 7. 用語集(このゲーム・このドキュメントに出てくる言葉)

| 用語 | 意味 |
|---|---|
| 要件定義書 | これから作るものの仕様・条件をまとめた文書 |
| 機能要件 | プログラムが備えるべき「できること」の条件 |
| 非機能要件 | 動作環境や使いやすさなど、「できること」以外の品質条件 |
| レーン | 手が落下してくる縦方向の通り道(このゲームでは3本) |
| 判定ライン | これを敵の本体が通過するとミスになる、レーンの下端の赤い線 |
| 対象 | 今のジャンケン判定が適用される、一番落下が進んでいる1体の敵 |
| 残機 | あと何回失敗できるかを表す数(ハートの数で表示。どのモードも初期値10) |
| コンボ | 「勝ち」が連続している回数。負け・ミスでリセットされる |
| タイムアタック | 制限時間やタイムを競うゲーム形式のこと |

---

## 8. 今後の拡張アイデア(実装は未定)

- ハイスコア・ベストタイムをブラウザに保存する機能
- 難易度上昇モードで「現在の速度レベル」を表示する
- コンボによるスコア倍率ボーナス
- 対象の敵を視覚的に強調表示する(枠で囲む、矢印を出すなど)
